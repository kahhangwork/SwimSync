// The Edit Child screen's module-level constants (docs/refactor/BATCH_FGH_PLAN.md,
// App L-F) — moved verbatim from app/(parent)/home/edit-child.tsx. The same list
// lives in features/add-child; kept feature-scoped on purpose (playbook §5 —
// don't lift to a shared module mid-refactor).
export const GENDER_OPTIONS = ["Male", "Female"];
